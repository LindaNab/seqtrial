globalVariables(unique(c(
  # construct_trial_n:
  "arm", "status_baseline", "tend", "treatment_lag1", "treatment_lag1_baseline", "trial", "tstart",
  # data_simulator:
  ".", "sum_A", "tt_A",
  # data_splitter:
  "tend", "tstart",
  # trial_constructor:
  "arm", "trial"
)))
# checkhelper::print_globals(quiet = TRUE)
